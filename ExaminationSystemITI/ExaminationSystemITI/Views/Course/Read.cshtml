@using ExaminationSystemITI.Models.Tables
@model List<Course>
@{
    ViewData["Title"] = "Dashboard";
    Layout = "_DashboardLayout";
}

<seciton class="w-100 d-flex flex-column" style="gap: 16px;">
    <select class="form-select w-100">
        <option class="form-control">Admin</option>
        <option class="form-control">Choice</option>
        <option class="form-control" selected>Course</option>
        <option class="form-control">Department</option>
        <option class="form-control">Exam</option>
        <option class="form-control">Instructor</option>
        <option class="form-control">Question</option>
        <option class="form-control">Student</option>
        <option class="form-control">Topic</option>
        <option class="form-control">User</option>
        <option class="form-control">Admin</option>
    </select>
    <a asp-action="create" asp-controller="course" class="btn btn-success poppins-semibold rounded" style="padding: 8px 16px; border: none;text-decoration: none; text-align: center;">
        Create
    </a>
    <table class="get-data table table-border table-hover">
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Grade</th>
                <th>Topics</th>
                <th>Departments</th>
                <th>Instructors</th>
                <th>Students</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Count() == 0)
            {
                <tr>
                    <td colspan="9">No Courses Exist!</td>
                </tr>
            }
            else
            {
                foreach (var course in Model)
                {
                    <tr>
                        <td>@course.Id</td>
                        <td>@course.Name</td>
                        <td>@course.Grade</td>
                        <td>
                            @foreach (var topic in course.Topics)
                            {
                                <span class="mr-1">
                                    @topic.Name,
                                </span>
                            }
                        </td>
                        <td>
                            @foreach (var department in course.Departments)
                            {
                                <span class="mr-1">
                                    @department.Name,
                                </span>
                            }
                        </td>
                        <td>
                            @foreach (var instructor in course.Instructors)
                            {
                                <span class="mr-1">
                                    @instructor.FirstName,
                                </span>
                            }
                        </td>
                        <td>
                            @foreach (var student in course.CourseStudents)
                            {
                                <span class="mr-1">
                                    @student.StudentId,
                                </span>
                            }
                        </td>
                        <td>
                            <a asp-action="Edit" asp-controller="Course" asp-route-id="@course.Id" class="btn btn-primary poppins-semibold rounded" style="padding: 8px 16px; border: none;transition-duration: 0.25s; text-decoration: none; text-align: center;">
                                Update
                            </a>
                            <a asp-action="Delete" asp-controller="Course" asp-route-id="@course.Id" class="btn btn-danger poppins-semibold rounded" style="padding: 8px 16px; border: none;text-decoration: none; text-align: center;" onclick="return confirm('Are you sure?')">
                                Delete
                            </a>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</seciton>
